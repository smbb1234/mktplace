[pytest]
; Only collect tests located under the tests/ directory to avoid collecting
; helper scripts or one-off scripts in other folders (e.g., scripts/test_api.py).
python_files = tests/*.py tests/**/*.py
testpaths = tests
